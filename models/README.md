# Put your model directories here.
#
# Each subdirectory should be a complete HuggingFace model
# (config.json, *.safetensors, tokenizer files, etc.)
#
# Examples:
#   ln -s /path/to/Qwen3-27B-AWQ models/Qwen3-27B-AWQ
#   ln -s /path/to/Qwen3-27B-GPTQ models/Qwen3-27B-GPTQ
#
# The launcher auto-discovers all subdirectories and presents them in the menu.
# (This file is tracked so the directory exists in git. Models themselves are gitignored.)
